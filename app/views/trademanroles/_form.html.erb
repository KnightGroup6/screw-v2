<%= form_for(@trademanrole) do |f| %>
  <% if @trademanrole.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trademanrole.errors.count, "error") %> prohibited this trademanrole from being saved:</h2>

      <ul>
      <% @trademanrole.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
