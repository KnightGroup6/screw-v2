<%= form_for(@trademanprofilepage) do |f| %>
  <% if @trademanprofilepage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trademanprofilepage.errors.count, "error") %> prohibited this trademanprofilepage from being saved:</h2>

      <ul>
      <% @trademanprofilepage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :parent_id %><br>
    <%= f.collection_select :parent_id, Trademanprofilepage.order(:name), :id, :name, include_blank: true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
