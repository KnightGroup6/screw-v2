<%= form_for(@jobstartdate) do |f| %>
  <% if @jobstartdate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@jobstartdate.errors.count, "error") %> prohibited this jobstartdate from being saved:</h2>

      <ul>
      <% @jobstartdate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
